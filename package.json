{
  "name": "ergon-foundation-platform",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "concurrently -k -n client,server -c cyan,green \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:web": "npm run dev --workspace @ergon/web",
    "dev:api": "npm run start:dev --workspace @ergon/api",
    "build": "npm run build --workspace @ergon/web && npm run build --workspace @ergon/api",
    "typecheck": "npm run typecheck --workspace @ergon/web && npm run typecheck --workspace @ergon/api"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
